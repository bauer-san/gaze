# Jetson-specific dependency guidance
# This file documents recommended installs for Jetson boards. It is *not* intended
# to be pip-installed directly; follow the notes below.

# System / apt (recommended)
python3-opencv            # contains OpenCV built for the platform (use apt)
gstreamer1.0-tools
gstreamer1.0-plugins-base
gstreamer1.0-plugins-good
v4l-utils

# Python packages (pip) — prefer lightweight or ONNX/TensorRT-friendly packages
numpy
PyYAML

# Optional (may require building from source)
# mediapipe  # build from source or use an alternative TensorRT/ONNX pipeline
# open3d    # build from source if needed
# pyrealsense2  # use distribution-specific instructions

# Notes:
# - Do not pip install opencv-python wheel on Jetson (x86 wheel). Use the distro package or build OpenCV with CUDA support.
# - For heavy ML models, convert to ONNX and then to TensorRT (FP16) for best performance.